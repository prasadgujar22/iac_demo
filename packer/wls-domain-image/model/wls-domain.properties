# WDT variable file. Values are injected at build time; secrets come from
# Jenkins credentials or Terraform, never committed.
domain.name=wls_domain
cluster.name=WLSCluster
cluster.size=2
admin.username=prasadmin
admin.password=CHANGE_ME_AT_BUILD_TIME
